import { SoftwareDeveloperType } from './software-developer-type';

describe('SoftwareDeveloperType', () => {
  it('should create an instance', () => {
    expect(new SoftwareDeveloperType()).toBeTruthy();
  });
});
